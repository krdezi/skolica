save article funkcija

$articleData=['title'=>@params['title', ]]

$articles = [];
    foreach (getArticles() as $articles){
        if ($article->title === $articleData['title']) {
            $articles[] = $articleData;

        } else {
            $articles[] =$article;
        }

    }

