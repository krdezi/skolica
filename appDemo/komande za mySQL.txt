SELECT SUM(`age`) FROM `user`;
SELECT AVG(`age`) FROM `user`;
SELECT MAX(`age`) FROM `user`;
SELECT MIN(`age`) FROM `user`;

// ALIASI za polje 
SELECT SUM(`age`) AS suma FROM `user`;
// ALIASI za tabelu, uglavnom prvo slovo imena tabele, pise se bez AS, samo to prvo slovo
SELECT SUM(`age`) FROM `user` U;

// spajanje tabela
SELECT * FROM `article` JOIN `user` USING(`userId`);

//prikaz samo stvari iz article tabele, bez detalja o useru
SELECT article.* FROM `article` JOIN `user` USING(`userId`) WHERE user.status = 1;

//prikazuje samo title ili sta vec izaberemo
SELECT article.title FROM `article` JOIN `user` USING(`userId`) WHERE user.status = 1;

SKUPOVI

